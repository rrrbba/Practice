function sumOfTwo (a, b, v) {
    //loop through a
        //loop through b
            //sum value from a + value from b
            //if the sum === v
                //return true
    //return false

    //use an object to keep track of diffs between v and all the values from one of the lists
    //loop through a 
        //diff = v-a
        //store diff in an object because O(1) and array is O(n)
    //look through the other array (b)
        //check to see if the current element is contained in the dict
        //return true if we find it
    //return false
}

//loops not nested you will add them
//nested loops multiply them